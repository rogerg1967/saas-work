/**
 * Constants for user roles within the application
 * These values must match the enum validation in the User model
 */
const USER_ROLES = {
  ADMIN: 'admin',
  ORGANIZATION_MANAGER: 'organization_manager',
  TEAM_MEMBER: 'team_member'
};

module.exports = USER_ROLES;